# Xcode environment variables for React Native
export NODE_BINARY=$(command -v node)
export NODE_ARGS="--max-old-space-size=8192"
export BUNDLE_COMMAND="bundle"

# Monorepo support: point Metro to the correct working directory
export REACT_NATIVE_PATH="${PODS_ROOT}/../../../node_modules/react-native"
